# Example environment variables for the AI-Enhanced QKD pipeline
# The pipeline does not require environment variables by default,
# but you can override defaults with these values if you extend it.

QKD_CONFIG_PATH=config/pipeline.json
QKD_OUTPUT_DIR=results
